def connected_components(graph: dict[str, set[str]]) -> list[set[str]]:
    """그래프의 연결 요소를 깊이 우선 탐색으로 나누어 반환합니다."""
    raise NotImplementedError("LEARNER_TASK")


def has_cycle(graph: dict[str, set[str]]) -> bool:
    """무방향 그래프에 사이클이 있는지 반환합니다."""
    raise NotImplementedError("LEARNER_TASK")
