# 워크북 10: Floyd--Warshall와 모든 쌍 최단 경로

정점 순서를 `A, B, C`로 두고 다음 유향 가중 그래프를 생각하세요.

`A→B(1), B→C(2), A→C(10)`

1. 처음 거리 행렬을 쓰세요. 이어서 중간 정점으로 `B`를 허용한 뒤, 어느 칸이 어떻게 바뀌는지 한 번 갱신하세요.
2. `D_k(i, j)`가 “중간 정점으로 처음 `k`개만 허용한 최단 거리”라는 명제가 성립함을 귀납적으로 설명하세요. 새 정점 `k`를 지나는 경우와 지나지 않는 경우를 구분하세요.
3. 알고리즘이 끝난 뒤 `D(v, v) < 0`인 정점이 있으면 왜 음수 사이클이 존재하나요? 그때 왜 최단 거리가 정의되지 않나요?
