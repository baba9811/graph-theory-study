# 13. 이분 매칭과 증강 경로 워크북

왼쪽 정점이 `A, B`, 오른쪽 정점이 `1, 2`이고 간선이 `A-1`, `A-2`, `B-1`이라고 합시다. 현재 매칭이 `A-1`일 때 `B`에서 시작하는 증강 경로를 하나 쓰세요.

그 증강 경로로 재배정하기 전과 후의 매칭을 각각 쓰세요.

각 왼쪽 정점에서 `augment(left_vertex, set())`처럼 새로운 방문 집합을 만드는 이유를 설명하세요.
