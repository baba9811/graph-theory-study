# 18. 교통망 분석 종합 실습 워크북

다음 교통망에서 `start="A"`, `end="C"`로 분석합니다.

```python
graph = {
    "A": {"B": 1, "C": 5},
    "B": {"A": 1, "C": 2},
    "C": {"A": 5, "B": 2},
}
```

## 1. 알고리즘 선택

코드를 작성하기 전에 표를 완성하세요.

| 분석 질문 | 사용할 기존 함수 | 선택 이유 |
|---|---|---|
| 전체 그래프가 연결되었는가? | LEARNER_TASK | LEARNER_TASK |
| `A`에서 모든 정점까지의 최소 비용은? | LEARNER_TASK | LEARNER_TASK |
| 부모 딕셔너리에서 `A → C` 경로는? | LEARNER_TASK | LEARNER_TASK |
| 모든 도시를 잇는 최소 건설 비용은? | LEARNER_TASK | LEARNER_TASK |

## 2. 예상 보고서 손계산

`A`에서 `C`로 가는 후보 경로의 비용과 최소 신장 트리(Minimum Spanning Tree, MST)가 고르는 간선을 손으로 계산한 뒤 예상 보고서를 완성하세요.

```python
LEARNER_TASK
```

## 3. 입력 검증의 이유

다음 입력을 왜 거부해야 하는지 각각 설명하고, 어느 검사가 책임지는지 쓰세요.

1. `start` 또는 `end`가 `graph`의 키가 아닌 경우
2. 이웃으로 적힌 정점이 `graph`의 키가 아닌 경우
3. `A → B`의 가중치는 1인데 `B → A`가 없거나 가중치가 다른 경우
4. 간선 가중치가 음수인 경우

LEARNER_TASK

## 4. 테스트 사례 설계

다음 세 동작을 검증하는 입력과 예상 결과 또는 예외를 쓰세요.

1. 연결된 교통망에서 네 보고서 값이 모두 계산된다.
2. 목적지가 다른 연결 요소에 있으면 `route`, `distance`, `infrastructure_cost`가 올바르게 보고된다.
3. 비대칭 간선은 `ValueError`를 발생시킨다.

LEARNER_TASK

## 5. 정당성과 복잡도

연결 요소, Dijkstra, 경로 복원, Kruskal의 보장을 이어 붙여 네 반환값이 정확함을 설명하세요. `source < target`이 같은 무방향 간선을 정확히 한 번 남기는 이유도 포함하세요. 이어서 정점 수 `|V|`와 무방향 간선 수 `|E|`를 사용해 전체 시간과 추가 공간 복잡도를 쓰세요.

LEARNER_TASK

## 6. 최종 성찰

이 분석기가 해결하지 않는 그래프 문제를 하나 이름 붙이고, 현재 네 반환값만으로 그 문제의 답을 알 수 없는 이유를 설명하세요.

LEARNER_TASK
